<div class="videochat">
    <style scoped>
        #video-grid {
            position: relative;
            top: -200px;
            display: grid;
            grid-template-columns: repeat(auto-fill, 100px);
            grid-auto-rows: 150px;
        }

        video {
            width: 100%;
            height: 100px;
            object-fit: cover;
        }

        .mt-4 {
            margin-top: 16px;
        }

        .btn {
            color: #fff;
            background: teal;
            display: inline-block;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        .btn:hover {
            background: rgb(0, 97, 97);
        }
    </style>
    <div id="video-grid"></div>
    <div class="mt-4">
        <div class="video-controls" id="video-controls-my">
            <button class="btn mute-btn" data-action="mute">mute</button>
            <button class="btn hide-btn" data-action="hide" data-hidden="0">hide</button>
        </div>
        <div id="currentRoom">None</div>
        <div id="joinRoomTriggers">
            <button data-room="heyM0de" class="btn">move to heyM0de</button><br>
            <button data-room="helloMoto" class="btn">move to helloMoto</button>
        </div>
    </div>
</div>